# 全站手机界面实施清单

## 目标与范围

沿用现有 URL、权限、Server Actions、审计和通知规则，实现全站功能等价的手机界面。采用同路由响应式壳层和必要的窄屏呈现编排；同一业务表单、草稿和画布状态保持单一来源。覆盖采购报销、项目任务与审批、会议与模板、通知、物资、反馈、个人中心及管理后台。

本任务属于共享 UI、跨模块交互改造。验收包括桌面 `1440×1000`、手机 `393×851`、最窄 `360px`、平板与布局断点；不得以隐藏操作或整页横向滚动实现适配。D110 直连打印仍依赖浏览器 Web Serial 能力，不承诺手机原生打印；保留明确说明、二维码下载及链接复制。

## 实施阶段

- [x] 0. 核对代码现状、仓库规则和用户已确定的产品范围，建立本清单。
- [ ] 1. 公共基础：紧凑顶部栏、系统/模块导航抽屉、移除主内容固定最小宽度、窄屏弹层、触摸目标与安全区。
- [ ] 2. 采购报销：列表、筛选、明细编辑、详情操作、导入导出、统计与汇总的手机适配和回归。
- [ ] 3. 项目管理：列表、详情、Composer、审批、会议、通知与时间画布；补齐无需双击、Shift 或拖动的显式操作。
- [ ] 4. 其他模块：物资/扫码、反馈、个人中心、首页和管理后台；保留角色差异与全部业务入口。
- [ ] 5. 视觉审查：通过官方隔离 Playwright runner 生成桌面与手机截图，逐张检查并修正；覆盖长内容、弹层、空/错误/只读状态和触摸交互。
- [ ] 6. 完成门禁：全任务 diff 自查、独立代码/视觉审查、`npm run check`、完整 `npm run test:e2e`、`npm run build`。
- [ ] 7. 同步 README、技术说明和测试说明，记录实际验证结果与设备限制。

## 关键设计决策

- `1024px` 以下使用紧凑导航和可访问抽屉，入口数据、顺序、分组、权限与未读数与桌面共享；`768px` 以下以单列内容为主。
- 优先通过布局重排复用现有组件；仅复杂列表、详情面板需要专用呈现。避免同时挂载两套有副作用的表单/画布，改变屏宽不丢失输入。
- 表格和时间画布可在有标签、有边界的容器内滚动；页面和主内容整体不能成为宽桌面画布。禁止通过全局裁切掩盖不可达操作。
- 触摸环境主要操作目标至少 `44×44px`；输入、错误提示、焦点、抽屉、图片预览、版本提示和软键盘不得相互遮挡。
- 画布触摸默认浏览/选择，编辑使用显式详情和日期时间控件；节点多选与批量移动支持无修饰键操作，并复用原有校验和撤销历史。
- 保留筛选、分页、深链、滚动上下文和草稿语义；继续执行服务端能力校验、并发控制与通知 outbox。

## 验证与视觉证据

自动化只使用 `npm run test:e2e -- <相关 spec>` 及完整官方入口，保留隔离数据库、受控端口和飞书禁发保护。不并发运行争用数据库/端口的检查。截图与报告仅放入被忽略的测试产物目录，不提交图片、cookie 或 storage state。

| 范围 | 必须检查的场景 | 状态 |
| --- | --- | --- |
| 公共壳层 | 手机/桌面导航、抽屉焦点、断点切换、长标题、短窗口 | 待验证 |
| 采购 | 筛选/详情/明细编辑、审批与附件、汇总滚动、弹层 | 待验证 |
| 项目与画布 | 任务/审批/会议/通知、节点选择与编辑、只读投入、失败重试 | 待验证 |
| 物资与管理 | 扫码确认、照片、硬件不支持提示、权限和预算配置 | 待验证 |
| 反馈与个人 | 会话切换、输入保留、图片、签名、长记录 | 待验证 |
| 全站 | 无错误覆盖层/未捕获异常、无整页溢出、主要操作在视口内可达 | 待验证 |

浏览器自动化的窄屏与触摸模拟不等于真机认证。iOS Safari、Android Chrome、飞书内置浏览器的真实 OAuth、相册、下载和硬件兼容性，只有实际完成检查后才记录通过。

## 执行记录

- 初始基线：`2d94d7f`，工作区干净。用户授权由主代理编排实施，并要求视觉审查。
- 当前：阶段 1–4 分工推进，测试命令由主代理统一串行调度。
